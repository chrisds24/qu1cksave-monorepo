import SingleJobSkeleton from "@/components/loading/singleJobSkeleton";

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <SingleJobSkeleton />
}